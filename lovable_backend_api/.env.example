# Backend configuration example
# Request these values from the deployment orchestrator and set them in the runtime .env
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/lovable
FRONTEND_ORIGIN=http://localhost:3000
BACKEND_PORT=3001
STORAGE_DIR=/tmp/lovable_storage
PREVIEW_BASE_URL=http://localhost:3001
JWT_SECRET=replace-with-a-secure-random-secret
